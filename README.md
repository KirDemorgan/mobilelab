# iOS Expo reference

Minimal Expo SDK 57 application configured for iOS only.

## Run

```powershell
npm install
npm run ios
```

Start in `App.tsx`. Add an iOS bundle identifier and icon in `app.json` before creating a standalone build.
